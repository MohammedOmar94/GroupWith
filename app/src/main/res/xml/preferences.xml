<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen  xmlns:android="http://schemas.android.com/apk/res/android">
    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch2"
        android:title="@string/pref_title_enable_push_notifications" />


    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:textColor="@android:color/black"
        android:capitalize="words"
        android:inputType="textCapWords"
        android:key="display_name"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_display_name" />

    <Preference
        android:key="update_location"
        android:title="Update Location"></Preference>

    <Preference
        android:key="sign_out"
        android:title="Sign out"></Preference>

    <Preference
        android:key="delete_account"
        android:title="Delete account"></Preference>


</PreferenceScreen>